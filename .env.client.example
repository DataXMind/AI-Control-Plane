# Client-only environment — copy to .env or export in your shell.
# Use when ACP is already running somewhere else (CONNECT door).
# See docs/QUICKSTART.md §2

ACP_API_URL=http://localhost:8000

# Do NOT set on client machines (API host only):
# ACP_CONFIG_DIR=
# ACP_DATA_DIR=
# ACP_REDIS_URL=
