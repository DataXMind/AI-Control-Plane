"""Config YAML is loaded via ``config.loader`` — no duplicate Pydantic mirror (D2).

Use ``ai_control_plane.config.loader`` and ``ai_control_plane.core.models`` for
validated runtime config and domain types.
"""
